language: node_js
node_js:
  - "7"

script:
  - yarn build

cache:
  yarn: true
  directories:
    - "node_modules"

deploy:
  - provider: pages
    local_dir: out
    skip_cleanup: true
    github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
    on:
      branch: master
